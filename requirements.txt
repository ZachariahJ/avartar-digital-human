edge-tts
openai
funasr
python-dotenv
scipy
starlette
uvicorn
onnxruntime   # CPU-only; for smart-turn v3 EOU (semantic end-of-utterance) in modules/eou.py
pytest        # dev/test only: tests/ (deterministic clinical core + pipeline integration)
